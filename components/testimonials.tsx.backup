"use client";

import { motion, useScroll, useTransform } from "framer-motion";
import { useState, useRef, useEffect } from "react";
import ArrowBackIosNewIcon from "@mui/icons-material/ArrowBackIosNew";
import ArrowForwardIosIcon from "@mui/icons-material/ArrowForwardIos";
import FormatQuoteIcon from "@mui/icons-material/FormatQuote";

export default function Testimonials() {
  const [hoveredIndex, setHoveredIndex] = useState<number | null>(null);
  const [currentIndex, setCurrentIndex] = useState(0);
  const [isAtStart, setIsAtStart] = useState(true);
  const [isAtEnd, setIsAtEnd] = useState(false);
  const containerRef = useRef<HTMLElement>(null);
  const scrollContainerRef = useRef<HTMLDivElement>(null);
  const { scrollYProgress } = useScroll({
    target: containerRef,
    offset: ["start end", "end start"],
  });

  const testimonials = [
    {
      name: "Faiza Abou Zaher",
      role: "Client",
      image: "/images/testimonials/faiza-abou-saher.png",
      quote:
        "Working with this developer was an absolute pleasure. The attention to detail and innovative solutions exceeded our expectations.",
      rating: 5,
      color: "from-purple-500 to-pink-500",
    },
    {
      name: "Rashad Al Rifai",
      role: "Client",
      image: "/images/testimonials/Rashad-al-rifai.png",
      quote:
        "Exceptional work! The project was delivered on time with outstanding quality. Highly recommend for any web development needs.",
      rating: 5,
      color: "from-blue-500 to-cyan-500",
    },
    {
      name: "Shimaa Metwaly",
      role: "Client",
      image: "/images/testimonials/Shimaa-metwaly.png",
      quote:
        "Professional, creative, and incredibly skilled. Transformed our vision into a beautiful, functional reality.",
      rating: 5,
      color: "from-pink-500 to-purple-500",
    },
    {
      name: "Youssef Boukhary",
      role: "Client",
      image: "/images/testimonials/Youssef-Boukhary.png",
      quote:
        "Outstanding technical expertise and communication. The solutions provided were both elegant and scalable.",
      rating: 5,
      color: "from-green-500 to-teal-500",
    },
    {
      name: "Abnoub Talaat",
      role: "Client",
      image: "/images/testimonials/abnoub-talaat.png",
      quote:
        "Incredible results! The website not only looks amazing but also performs exceptionally. A true professional.",
      rating: 5,
      color: "from-orange-500 to-red-500",
    },
    {
      name: "Eman Osama Mensiy",
      role: "Client",
      image: "/images/testimonials/eman-osama-mensiy.png",
      quote:
        "Best decision we made for our project. Creative problem-solving and dedication to excellence in every aspect.",
      rating: 5,
      color: "from-indigo-500 to-blue-500",
    },
    {
      name: "Hanan Sayed",
      role: "Client",
      image: "/images/testimonials/hanan-sayed.png",
      quote:
        "Exceptional developer with great communication skills. Delivered beyond what we asked for.",
      rating: 5,
      color: "from-purple-500 to-pink-500",
    },
    {
      name: "Hoda Yousry",
      role: "Client",
      image: "/images/testimonials/hoda-yousry.png",
      quote:
        "Highly skilled and professional. The quality of work is outstanding and the delivery was prompt.",
      rating: 5,
      color: "from-blue-500 to-cyan-500",
    },
    {
      name: "Kirolls Nadi Ebrahim",
      role: "Client",
      image: "/images/testimonials/kirolls-nadi-ebrahim.png",
      quote:
        "Amazing experience working together. The project exceeded all expectations with innovative solutions.",
      rating: 5,
      color: "from-pink-500 to-purple-500",
    },
    {
      name: "Mohamed Sameer",
      role: "Client",
      image: "/images/testimonials/mohamed-sameer.png",
      quote:
        "Professional and talented developer. The attention to detail and quality is remarkable.",
      rating: 5,
      color: "from-green-500 to-teal-500",
    },
    {
      name: "Reda Salem",
      role: "Client",
      image: "/images/testimonials/reda-salem.png",
      quote:
        "Outstanding work from start to finish. Highly recommend for anyone looking for quality development.",
      rating: 5,
      color: "from-orange-500 to-red-500",
    },
    {
      name: "Wafaa Hamdy",
      role: "Client",
      image: "/images/testimonials/wafaa-hamdy.png",
      quote:
        "Excellent developer with great vision. The project was completed with precision and creativity.",
      rating: 5,
      color: "from-indigo-500 to-blue-500",
    },
    {
      name: "Faize Abou Zaher",
      role: "Client",
      image: "/images/testimonials/Faize-Abou-zaher.png",
      quote:
        "Fantastic collaboration! Brought fresh ideas and delivered exceptional quality throughout the project.",
      rating: 5,
      color: "from-purple-500 to-pink-500",
    },
  ];

  const y = useTransform(scrollYProgress, [0, 1], [100, -100]);

  // Check scroll position
  const checkScrollPosition = () => {
    if (scrollContainerRef.current) {
      const { scrollLeft, scrollWidth, clientWidth } =
        scrollContainerRef.current;
      setIsAtStart(scrollLeft <= 10);
      setIsAtEnd(scrollLeft + clientWidth >= scrollWidth - 10);

      // Calculate current index based on scroll position
      const cardWidth = 516; // Approximate card width including gap
      const index = Math.round(scrollLeft / cardWidth);
      setCurrentIndex(Math.min(index, testimonials.length - 1));
    }
  };

  useEffect(() => {
    const container = scrollContainerRef.current;
    if (container) {
      container.addEventListener("scroll", checkScrollPosition);
      checkScrollPosition(); // Initial check
      return () => container.removeEventListener("scroll", checkScrollPosition);
    }
  }, []);

  // Scroll functions for carousel
  const scroll = (direction: "left" | "right") => {
    if (scrollContainerRef.current) {
      const scrollAmount = 600;
      const newScrollLeft =
        scrollContainerRef.current.scrollLeft +
        (direction === "left" ? -scrollAmount : scrollAmount);

      scrollContainerRef.current.scrollTo({
        left: newScrollLeft,
        behavior: "smooth",
      });
    }
  };

  // Scroll to specific testimonial
  const scrollToIndex = (index: number) => {
    if (scrollContainerRef.current) {
      const cardWidth = 516; // Card width + gap
      scrollContainerRef.current.scrollTo({
        left: cardWidth * index,
        behavior: "smooth",
      });
    }
  };

  return (
    <section
      id="testimonials"
      ref={containerRef}
      className="min-h-screen py-32 px-4 md:px-8 relative overflow-hidden bg-linear-to-b from-background via-background/95 to-background"
    >
      {/* Animated Background Grid */}
      <div className="absolute inset-0 bg-[linear-gradient(rgba(139,92,246,0.03)_2px,transparent_2px),linear-gradient(90deg,rgba(139,92,246,0.03)_2px,transparent_2px)] bg-size-[80px_80px]" />

      {/* Floating Orbs */}
      <motion.div
        style={{ y }}
        className="absolute top-20 left-10 w-96 h-96 bg-purple-500 rounded-full blur-[150px] opacity-10"
      />
      <motion.div
        style={{ y: useTransform(scrollYProgress, [0, 1], [-80, 80]) }}
        className="absolute bottom-20 right-10 w-80 h-80 bg-pink-500 rounded-full blur-[120px] opacity-10"
      />

      {/* Floating Particles */}
      {[...Array(12)].map((_, i) => (
        <motion.div
          key={i}
          className="absolute w-2 h-2 bg-purple-400/20 rounded-full"
          style={{
            left: `${Math.random() * 100}%`,
            top: `${Math.random() * 100}%`,
          }}
          animate={{
            y: [0, -30, 0],
            opacity: [0.2, 0.8, 0.2],
            scale: [1, 1.5, 1],
          }}
          transition={{
            duration: Math.random() * 3 + 2,
            repeat: Infinity,
            delay: Math.random() * 2,
          }}
        />
      ))}

      <div className="max-w-7xl mx-auto relative z-10">
        {/* Header */}
        <motion.div
          className="mb-16 text-center"
          initial={{ opacity: 0, y: 50 }}
          whileInView={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.8 }}
          viewport={{ once: true }}
        >
          <motion.h2
            className="text-5xl md:text-6xl font-bold mb-4"
            initial={{ opacity: 0, y: 20 }}
            whileInView={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.8, delay: 0.3 }}
            viewport={{ once: true }}
          >
            <span className="bg-linear-to-r from-purple-400 via-pink-400 to-purple-400 bg-clip-text text-transparent">
              Testimonials
            </span>
          </motion.h2>

          <motion.div
            className="w-24 h-1.5 bg-linear-to-r from-purple-500 to-pink-500 rounded-full mx-auto"
            initial={{ width: 0 }}
            whileInView={{ width: 96 }}
            transition={{ duration: 0.8, delay: 0.4 }}
            viewport={{ once: true }}
          />

          <motion.p
            className="text-muted-foreground mt-6 text-lg max-w-2xl mx-auto"
            initial={{ opacity: 0 }}
            whileInView={{ opacity: 1 }}
            transition={{ duration: 0.8, delay: 0.5 }}
            viewport={{ once: true }}
          >
            Recommendations from clients and collaborators
          </motion.p>
        </motion.div>

        {/* Carousel Container */}
        <div className="relative">
          {/* Left Arrow */}
          <motion.button
            onClick={() => scroll("left")}
            whileHover={{ scale: isAtStart ? 1 : 1.1 }}
            whileTap={{ scale: isAtStart ? 1 : 0.9 }}
            disabled={isAtStart}
            className={`absolute left-0 top-1/2 -translate-y-1/2 z-20 bg-linear-to-r from-purple-500 to-pink-500 text-white p-3 rounded-full shadow-lg transition-all duration-300 hidden md:flex items-center justify-center ${
              isAtStart
                ? "opacity-30 cursor-not-allowed"
                : "hover:shadow-2xl hover:shadow-purple-500/50 cursor-pointer"
            }`}
            aria-label="Scroll left"
          >
            <ArrowBackIosNewIcon className="text-xl" />
          </motion.button>

          {/* Right Arrow */}
          <motion.button
            onClick={() => scroll("right")}
            whileHover={{ scale: isAtEnd ? 1 : 1.1 }}
            whileTap={{ scale: isAtEnd ? 1 : 0.9 }}
            disabled={isAtEnd}
            className={`absolute right-0 top-1/2 -translate-y-1/2 z-20 bg-linear-to-r from-purple-500 to-pink-500 text-white p-3 rounded-full shadow-lg transition-all duration-300 hidden md:flex items-center justify-center ${
              isAtEnd
                ? "opacity-30 cursor-not-allowed"
                : "hover:shadow-2xl hover:shadow-purple-500/50 cursor-pointer"
            }`}
            aria-label="Scroll right"
          >
            <ArrowForwardIosIcon className="text-xl" />
          </motion.button>

          {/* Scrollable Carousel */}
          <div
            ref={scrollContainerRef}
            className="flex gap-6 overflow-x-auto scroll-smooth pb-4 px-2 scrollbar-hide items-start"
            style={{
              scrollbarWidth: "none",
              msOverflowStyle: "none",
            }}
          >
            {testimonials.map((testimonial, index) => (
              <motion.div
                key={index}
                initial={{ opacity: 0, x: 50 }}
                whileInView={{ opacity: 1, x: 0 }}
                transition={{ duration: 0.5, delay: index * 0.05 }}
                viewport={{ once: true, margin: "-100px" }}
                onHoverStart={() => setHoveredIndex(index)}
                onHoverEnd={() => setHoveredIndex(null)}
                className="group relative shrink-0 max-w-[90vw] md:max-w-[600px]"
              >
                {/* Gradient Glow */}
                <motion.div
                  className={`absolute -inset-2 bg-linear-to-r ${testimonial.color} rounded-2xl blur-xl opacity-30 group-hover:opacity-60 transition-opacity duration-500`}
                  animate={{
                    scale: hoveredIndex === index ? 1.05 : 1,
                  }}
                />

                {/* Card */}
                <motion.div
                  whileHover={{ y: -4 }}
                  transition={{ duration: 0.3 }}
                  className="relative bg-card/80 backdrop-blur-xl rounded-2xl overflow-hidden border-2 border-transparent group-hover:border-opacity-100 shadow-xl group-hover:shadow-2xl transition-all duration-500"
                >
                  {/* Quote Icon - Bottom Right */}
                  <div className="absolute bottom-4 right-4 z-20 rotate-180">
                    <motion.div
                      className={`w-12 h-12 bg-linear-to-r ${testimonial.color} rounded-full flex items-center justify-center opacity-90`}
                      animate={{
                        rotate:
                          hoveredIndex === index ? [180, 170, 190, 180] : 180,
                        scale: hoveredIndex === index ? 1.1 : 1,
                      }}
                      transition={{ duration: 0.5 }}
                    >
                      <FormatQuoteIcon className="text-white text-2xl" />
                    </motion.div>
                  </div>

                  {/* Auto-Sized Image Container */}
                  <div className="relative">
                    {/* Gradient Overlay */}
                    <div
                      className={`absolute inset-0 bg-linear-to-br ${testimonial.color} opacity-10 group-hover:opacity-20 transition-opacity duration-500 z-10`}
                    />

                    {/* Auto-Size Image - Preserves Natural Dimensions */}
                    <img
                      src={testimonial.image}
                      alt={`Recommendation from ${testimonial.name}`}
                      loading="lazy"
                      className="w-auto h-auto max-w-full max-h-[400px] object-contain relative z-0"
                    />

                    {/* Shine Effect */}
                    <motion.div
                      className="absolute inset-0 bg-linear-to-r from-transparent via-white/20 to-transparent -translate-x-full z-20"
                      animate={{
                        x: hoveredIndex === index ? "200%" : "-100%",
                      }}
                      transition={{ duration: 0.8 }}
                    />

                    {/* Name Overlay on Hover */}
                    <motion.div
                      className="absolute inset-x-0 bottom-0 bg-linear-to-t from-black/90 via-black/70 to-transparent p-6 z-30 opacity-0 group-hover:opacity-100 transition-opacity duration-300"
                      initial={{ opacity: 0 }}
                      whileHover={{ opacity: 1 }}
                    >
                      <h4 className="text-white font-bold text-lg text-center drop-shadow-lg">
                        {testimonial.name}
                      </h4>
                    </motion.div>
                  </div>
                </motion.div>
              </motion.div>
            ))}
          </div>

          {/* Pagination Dots */}
          <div className="mt-8 flex justify-center items-center gap-2">
            {testimonials.map((_, index) => (
              <motion.button
                key={index}
                onClick={() => scrollToIndex(index)}
                whileHover={{ scale: 1.2 }}
                whileTap={{ scale: 0.9 }}
                className={`transition-all duration-300 rounded-full ${
                  currentIndex === index
                    ? "w-8 h-3 bg-linear-to-r from-purple-500 to-pink-500"
                    : "w-3 h-3 bg-muted-foreground/30 hover:bg-muted-foreground/50"
                }`}
                aria-label={`Go to testimonial ${index + 1}`}
              />
            ))}
          </div>

          {/* Scroll Hint */}
          <motion.div
            className="mt-6 text-center text-muted-foreground text-sm md:hidden"
            initial={{ opacity: 0 }}
            whileInView={{ opacity: 1 }}
            transition={{ delay: 0.5 }}
            viewport={{ once: true }}
          >
            ← Swipe to see more →
          </motion.div>
        </div>
      </div>

      <style jsx>{`
        .scrollbar-hide::-webkit-scrollbar {
          display: none;
        }
      `}</style>
    </section>
  );
}
